- Logger
  + If single line is used and the function crashes it should print anyway without try-catch
  + Avoid trying to hook `{ configurable: false }` descriptors
  + Avoid hooking the same function twice

- Stream

- Timer
  + BUG If we start the timer, and then cancel it when we schedule it once it will start looping.

- Utils
  - Document and review new utils
    - print-jsx
    - download-url
    - CancellablePromise
  - Create more subfolders?
    - string
    - array
    - function
    - react
